/*
    Verificação de tipo estático
*/

const message = 'Hello TypeScript';

// Consigo mudar?
// message = 'TypeScript';

// Consigo execultar?
// message();

const user = {
  name: 'Pedro',
  email: 'email.com',
};

// Propriedade existe?
// console.log(user.avatar)
